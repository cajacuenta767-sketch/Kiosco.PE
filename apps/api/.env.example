# Puerto HTTP
PORT=3000
# Postgres (Neon, Supabase, Railway o local). Sin esta variable la API arranca en modo "sin base de datos".
DATABASE_URL=postgres://usuario:clave@localhost:5432/kiosco
# Orígenes permitidos para CORS, separados por coma
CORS_ORIGENES=http://localhost:5173,https://app.kiosco.pe
